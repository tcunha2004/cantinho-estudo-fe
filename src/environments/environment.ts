/**
 * Ambiente de desenvolvimento e de teste. O `angular.json` troca este arquivo
 * pelo `environment.prod.ts` no build de produção (`fileReplacements`).
 */
export const environment = {
  apiBaseUrl: 'http://localhost:3000',
};
